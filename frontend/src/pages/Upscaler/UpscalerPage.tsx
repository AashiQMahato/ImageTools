import { ZoomIn } from "lucide-react";
import { PagePlaceholder } from "@/components/common/PagePlaceholder";

export function UpscalerPage() {
    return <PagePlaceholder title="Upscaler" description="AI upscaling will live here. Increase resolution while preserving detail." icon={ZoomIn} />;
}
